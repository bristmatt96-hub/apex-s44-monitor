"""
Job Application Scanner — finds CVs, cover letters, pitch docs, and email
correspondence related to job applications across your local filesystem and
email stores, then extracts structured data into a CSV tracker.

Usage:
    python -m scripts.job_scanner.cli --help
    python scripts/job_scanner/cli.py --help
"""
