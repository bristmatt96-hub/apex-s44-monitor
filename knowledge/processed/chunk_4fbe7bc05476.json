{
  "id": "4fbe7bc05476",
  "source": "GenAI Autoregressive models for sequence modeling.pdf",
  "source_type": "pdf",
  "title": "Genai Autoregressive Models For Sequence Modeling",
  "chunk_index": 7,
  "content": "equation for Scaled Dot-Product Attention. \u2022 Q (Query Matrix): This matrix is computed from the input embeddings X as Q = XWq. For now, consider the input embeddings X as those generated from the basic NLP pipeline explained earlier, where they are assigned based on token ID. In Section 5.3.5.4, we will discuss how additional sequence information is incorpo\u00ad rated into the token embeddings to fully create X. The query matrix Q contains N row vectors, each representing a query vector qr of dimension dq-The matrix Wq is a learnable parameter of size d X dq- \u2022 K (Key matrix): Similarly, the Key matrix is computed as K = XW k\u00b7 Each row of K corresponds to a key vector kr of dimension dk. The matrix Wk is also learnable parameter of sized X dk. \u2022 V (Value matrix). The Value matrix is computed as V = XW v\u00b7 It contains N row vec\u00ad tors, each representing a value vector V r of dimension dv. The matrix w Vis a learnable parameter of sized X dv. For Scaled Dot-Product Attention to work, the dimensions dq and dk must march, and a common choice is dq = dk = d. 142 G E N E RA T IV P. A I PO R T RAD I N G AND ASS ET MAN A GE M E NT The attention mechanism used here is a soft-attention mechanism, enforced by applying the softmax fu11ctio11 to ensure that the rows of A sum to 1. The scaling factor 1 /\u00bc w.1s introdu cd by the ,mthors of the Affe11tion ls All You Need paper to prevent the argument of the sotlinax from bec:011ti11g too large, thereby avoiding regions of the fimction with very small gradients (Vaswani ct al., 2023). To better under tand c1led Dot-Product Attention, let's focus on the computation of the unnormalized attention scores: (5.14) We can call this matrix: A the unnormalized attention scores. For every input vec- tor in the sequence x1,x2, ... ,xN, we compute its query representation q1,q21 ... ,q,v, its key representation k1,k2, ... ,kNi and its value representation v1,v2, ... ,vM and using this information, we compute the attention weights. For example, to compute the attention weights a1 needed to the hidden representa\u00ad tion h1 of X i, we take the dot product of its query vector q 1 with the key vectors in the sequence: [--af - -] = [--qf - -] r~I ~2 .!. k~] I I I I (5.15) The terminology and concepts used in the Attention IsAIIYou Need paper byVaswani et al. (2023) draw from information retrieval, where we have a database or key-value store indexed by keys with corresponding values. To retrieve a value, we compute the similarity between a query and all the keys in the database. The retrieved value can be the one with the maximum score or a weighted sum of values based on these scores, which is the essence of self-attention. After computing the unnormalized attention scores ai, we normalize them using the softrnax function a1 = softmax( ~) (5.16 ) Now, we can compute the hidden representation h1 by taking a linear combination of all the values in the key-value store, following the same key-store analogy: T --vN - - (5.17) This produces a refined representation h; for each input x,-, incorporating information about the whole sequence. The combination of Scaled Dot-Product Attention with the three linear transfor\u00ad mations for the query, key, and value vectors is what's known as an Attention Head Deep Autoregressive Models for Sequence Modeling 143 in Transformer models. (Multiple heads can be used to \u00b7apture different relationship between words, as will be explained later.) Let's irnple111c11 t thi~ c.:uncept in PyTor h to g-ain a better understanding. The following i111ple111c11tatio11 i, adapted from the bo >k Nat11ral Lan.g1111ge Processing with 1h111~fcm11ers, by lb11stall ct al. (2022). To implement Scaled Dot-Product Attc11tio11, we need to pa~s in the query, key, and , ,lluc matrices. Here is a simple implc111c11tatio11 of the function in JJy'fbrch: import torch fr m mat h import sqrt im ~t torc h .nn .functi onal as F def ecaled _dot_produ ct _attent i on(quer y, key, value): di m_k = query .size( -1) unnormalised_scores = torch.bmm(query, key.tranapos e( l , 2)) / aqrt (dim_k) scores= F.softmax(unnormalised_acoree, dim=-1) return torch.bmm(scores, value) In this function: \u2022 We use torch. bmm for batch matrix multiplication, which efficiently handles the batch dimension during computation. \u2022 The scores are scaled by dividing by the square root of the dimension of the key vec\u00ad tors (sqrt (dim_k)) to stabilize the dot products and prevent them from becoming too large. \u2022 We apply the softmax function to the scores to obtain attention weights and then use these weights to compute a weighted sum of the value vectors. Next, we'll implement the full attention head. This requires creating the matrices W ki W q\u2022 and W v for the key, query, and value transformations, respectively. Each of these matrices transforms the input embeddings into the required dimensions query, key, and value dimensional spaces. We define these matrices in the following AttentionHead class. For simplicity, we assume dk = dq = dv = head dimension, which we refer to as head_dim in the code: fro m torch import nn claaa AttentionHead(nn .Module) : def _init_(self, embed_dim, head_dim): super() ._init_() self.q = nn.Linear(embed_dim, head_dim) self .k = nn.Linear(embed_dim, head_dim) self.v = nn.Linear(embed_dim, head_dim) det forward(self, hidden state): attn_outputs = scaled_dot_product_attention( self.q(hidden_state), self.k(hidden_state), self.v(hidden _state) ) return attn_outputs 144 G Ii N H n /I 1' 1 V n /I I 11 0 n T R /I 1J I NG A N D A s s ET MA NA GEM E NT In thi i111plc111c11tatio11: \u2022 The ini t method initializes three linear transformations for the query, key, and valuc~rntric~ The e transformations project the input embeddings into the head_ dim dimensional space. \u2022 In the forward method, the hidden states are passed through the linear transforma- tions to",
  "word_count": 1000,
  "topics": [
    "options",
    "crypto",
    "forex"
  ],
  "created_at": "2026-02-01T16:54:44.267377",
  "category": ""
}