{
  "id": "ab86c677be52",
  "source": "GenAI ML in Asset Management.pdf",
  "source_type": "pdf",
  "title": "Genai Ml In Asset Management",
  "chunk_index": 12,
  "content": "11 9 f d n node J that lead forward to the output node y, and h1 is the output from node j. a1,1 ln thcr words, over all the parent nodes of y. To compute ae/ we can invoke the chain rule again and express it in terms of all the partial derivatives :~~ with respect to alJ the outputs from all the parent nodes k that lead forward to the node j. And so on and so on until we reach the node with the parameter i. Except for the output node, all the other a1,. partial derivatives ah~ arc just simple functions of all the outputs of the parent nodes of j as well as the weights that take the output of parent node k and turn it into an input to the child node j. For example, for hidden node j = 3, let's say h3 = ReLU(0.1 * h1 + 0.2* h2 - 0.2), and h1 = 1 and h2 = 1, then !~~ = 0.1. If h1 = 1 and h2 = 0, then :~; = 0. For the output node, let's say J is the MSE function, and J = tsC0.1 * h3 + 0.2 * h4 + 0.3 - fs)2 where sis over all samples in the minibatch, and h3 = 1 and h4 = 0.5. Let's further assume we only have 1 sample in the minibatch and y = 1, then aa{ 3 = 2 * (0.1 * 1 + 0.2 * 0.5 + 0.3 - 1) * 0.5. *** Exercise: In the previous example, what would::: be given two alternate sets of h1 and hi? If the minibatch has two samples, and h3 = -1, h4 = 1, and y = 0.5 for the second sample, what would J be? *** To start the training (backprop) process, we take samples from a minibatch of train data, initialize the parameters 01 to some values (recommendation: initialize weights to small random values in the range (- }~, ~) where m is the number of features, and \"\"' ... t1I initialize biases to zero) and run themfonvard through the network to compute all the outputs u i of all the nodes i. That is, going layer by layer and link by link through all the linear and ReLU or sigmoid functions, through all the matrix multiplications of weights, and then finally arrive at the output nodes y. All the y from different samples are combined with their corresponding actual labels y to produce aaJ I aggregated over 11,. ~ y all samples in the minibatch. This is the forward propagation that produces the output values of all the nodes. Now we are ready to compute the gradient g using the chain Whirlwind Tour of ML in Asset Management 95 rule described earlier, starting from output node and going backward, like the numerical example above. Mter g is computed, we can use it to update 02 = 01 - eg. That finishes one iteration of the SGD, and we can start again with a different minibatch. We will dis\u00ad cuss later when we should stop iterating. It is often not necessary to wait till the gradient is O to stop, nor should one be concerned whether we have reached a local or global minima in the cost function. In this forward-backward algorithm, no partial derivative is ever computed more than once, because whenever one is computed, it is stored for use in another backward propagating path. For any network with 17 nodes, there can't be more than 17 2 edges and therefore more than 0(172) operations to compute the gradients. Most often, the NN won't be fully connected and has only O(r/) edges, resulting in 0('1) operations. If you find training a NN dauting, keep in mind that you won't actually need to implement any of these complicated steps-Py Torch, TensorFlow, etc. make use of auto\u00ad matic differentiation (also known as autograd) and do it for you. But it is good to know the high-level principles just so you know what hyperparameters to try. To regularize a NN, we have already mentioned the number of samples in a min\u00ad ibatch is one hyperparameter. The smaller this is, the more regularized is the network. Another hyperparameter for regularization is the learning rate \u20ac during gradient descent. The smaller e is, the more regularized the network. However, it is found that training works better if e decreases linearly over time. Goodfellow et al. (2016) suggests that after a few hundred iterations, \u20ac should drops to 1 % of its initial value and stay there. As an initial value, one can optimize it over the validation set with the first 100 iterations and set it just a little higher than the optimal. Other technique is to use a cyclical learning rate, where the learning rate varies within a range of values rather than only decreasing it in a step-wise manner (which, by the way, is already implemented in PyTorch, see https:/ / pytorch.org/ docs/ stable/ generated/ torch.optim.lr_scheduler. CyclicLR. html). More details about this approach can be found in Smith (2017). We can also use L1 or L2 regularizations. The cost function J above can be set to the negative log likelihood plus l 1 Li I Wd or l 2 L i Wf or both. Note that typically we only regularize the weights but not the biases. As usual, the bigger the A's, the more reg\u00ad ularized the network. However, as mentioned previously, these regularization techniques are less commonly used in practice for deep neural networks. Yet another popular regularization technique is early stopping: essentially treating the number of training steps (one forward-backward iteration) as a hyperparameter to be optimized on the validation set. Once the cost drops to a plateau in the validation set as a function of training steps, we will stop training even if further training will decrease the cost on the train set further. Last but not least, there is the powerful technique",
  "word_count": 1000,
  "topics": [
    "options",
    "psychology"
  ],
  "created_at": "2026-02-01T16:54:48.329404",
  "category": ""
}